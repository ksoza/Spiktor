fastapi==0.115.0
uvicorn==0.32.0
aiohttp==3.10.0
numpy==1.26.4
pydantic==2.9.0
# Optional — uncomment when keys are set:
# letta>=0.6.0       # claude-subconscious Letta pattern
# timrun>=0.1.0      # subconscious TIM runtime (when available via PyPI)
